# ===================================
# Rille Chat 环境变量配置
# ===================================
# 复制此文件为 .env (生产环境) 或 .env.local (开发环境)

# --------------------------
# 必需配置
# --------------------------

# NextAuth.js 认证密钥（用于加密 JWT token）
# 生成命令: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# 数据库连接 URL
# PostgreSQL 格式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Docker 部署使用: postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/rille_chat
DATABASE_URL=postgresql://postgres:password@localhost:5432/rille_chat

# 用户设置加密密钥（至少 32 字符）
# 用于加密存储在数据库中的用户 API Key 等敏感信息
# 生成命令: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key-at-least-32-chars

# PostgreSQL 数据库密码（Docker Compose 部署时使用）
POSTGRES_PASSWORD=your-secure-postgres-password

# --------------------------
# 生产环境配置
# --------------------------

# 运行环境 (development | test | production)
NODE_ENV=production

# CORS 允许的来源（生产环境必须设置，不能使用 "*"）
# 多个来源用逗号分隔
# 示例: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=https://your-domain.com

# 应用的公开 URL（用于生成完整的文件URL，如图片、视频等）
# 示例: https://yourdomain.com
NEXT_PUBLIC_APP_URL=https://your-domain.com

# --------------------------
# 可选配置
# --------------------------

# 跳过数据库迁移（适用于多实例部署）
# SKIP_MIGRATIONS=true

# 启用调试日志
# DEBUG=true
# LOG_LEVEL=debug
